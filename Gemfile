source "https://rubygems.org"

# Mirrors the gems GitHub Pages runs, so a local preview matches production.
gem "github-pages", group: :jekyll_plugins

# Needed on Ruby 3.0+ (no longer bundled with Ruby)
gem "webrick"
